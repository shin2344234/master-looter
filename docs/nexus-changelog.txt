For Crimson Desert 2.01.00 (exe 1.0.0.2760).

## Crashes

Fast travelling could kill the game. lsimo reported it and read the crash dump
before I did: a call through a null function pointer with this plugin on the
stack, twenty-four minutes into an ordinary session.

I have not fixed it. I thought I had, and testing says otherwise, so it is worth
being clear rather than quiet.

The theory was that work queued for the game's thread outlives a teleport and
then runs against objects the teleport freed. Anything queued is now thrown away
on a world change, and the log reports how many went. It has reported zero every
time, across many jumps with ore actively being worked, and the reason is that
the queue drains off the movement tick thousands of times a second, so nothing
is ever in it for more than about a millisecond. There was no queue to purge.

The purge stays because it costs nothing and closes a real hole. It is not the
cause of lsimo's crash, and that cause is still open.

What this release does add is a way to find it. If a fault ever happens while
the mod is driving one of the game's own state machines, the log now records
what that object looked like an instant before: its address, whether it was
still readable, its first bytes, and the class it names through its vtable. A
live object names itself. A freed one has had its memory reused and cannot. So
the next report of this settles in one line whether the pointer had gone stale
or whether the game faulted on its own business, instead of being argued about
from the outside.

A well run holds the same kind of pointer outside the queue, where the first fix
could not reach it, so it is dropped on a world change as well. Being straight
about the size of that one: a run finishes in about thirty-five milliseconds, so
the window is far too small to have been crashing anyone. It is defensive, not a
second cause. I described it as eleven seconds while writing this up, which came
from an older design that never shipped.

The second crash nobody had reported. Installing a hook wrote the redirect into
the game's function before saving the address of the original, and every
redirect calls that original immediately. A game thread arriving in that gap
called through nothing at all. The window is a few instructions wide, but hooks
go in while the world loads and the function in question runs thousands of times
a second, so it was small and pointed at something fast. This would have looked
like the game dying at startup for no reason, with nothing useful in the log.

## Looting nothing at all

Cmz4455 reported no auto looting as Damiane or Oongka. Their log has the scan
walking away from the player and settling on a steam engine, after which every
part of the train counted as the player's own gear and was skipped, so nothing
in reach was picked up.

The mod scores what to centre on, and a dressed character should beat a vehicle
easily. It lost because the train brought enough separate parents into range to
push the player out of a table that only holds thirty-two, and once the player
is not in the table none of the protections against exactly this apply. The
thing being scanned around can no longer be pushed out of its own table, and
something whose children have never once been identified as items cannot take
the centre from something whose have.

## New

LuxDragon asked after 1.5.0 whether specific creatures could be filtered. Now
they can, individually, by name.

The mod used to work out what a creature was by hunting through its memory for a
species word in a model name. That barely worked. Fish all arrive as "cd_fish"
and the small insects as "cd_effectmonster_normal", so across twelve recorded
sessions it named an exact species three times, and every rule you set was
quietly skipped because there was nothing to apply it to.

The creature has been carrying the answer the whole time. At status+0x30 there
is a row into the game's own character table, and that names it outright. So the
mod knows a Tench from a Ricefish before it decides anything.

Which makes item overrides work on them. Set Item_Tench to never on the Items
tab and tenches are left alone while every other fish is still caught. Nothing
new to learn: it is the same per-item control that has always been there, it
simply never had a species to act on before.

A few unusual creatures still fall back to the old model-name guess and answer
to the category switches only.

A key and a pad chord for taking owned goods, asked for by Ahplla. Unbound until
you set it, under General, Keys. Leaving that switch on earns a bounty, so it
wants turning on for a moment and off again, which a menu is a poor shape for.

## Smaller

Drawing a well no longer lets a vein you mined by hand collect the extra ore
from the tool bonus setting. Every well step was marking the vein bonus window
open, which is the one thing that setting promises cannot happen.

A settings file written before version 2 was migrated on every launch instead of
once, because the migration was never saved. That reverted a hand-edited
GatherUnknown on every start and left a spare backup each time, which pushed
real backups out of the twelve the Restore list keeps.

Loading a preset or restoring a backup now changes the menu language along with
everything else, instead of changing the setting and leaving the menu as it was.

The log names the mod that hooked DirectX before this one, rather than only
saying that something did. Faults in the log say "first chance" and carry a line
explaining that most of them are the game probing memory on purpose, because
they get pasted into reports as the crash and usually are not it. When a fault
does switch off ore breaking for a session, that now says so on screen instead
of the mod quietly getting worse.

The memory cache the scan leans on stopped working about five seconds into every
session, because a stale entry was left in front of the fresh one. It caches
again.

## Checksums

SHA-256 for 1.6.4:

2ffeb06540cdf70604cd98efbade930afbd95ffa5c275d3575f23fc276fe150e  MasterLooter-1.6.4-DMM.zip
392d2994d82151cac1efc86a5182395fdcdf8cd35d82840f3748e325222e0ac6  MasterLooter-1.6.4.zip
749f4803f5876a6b3f38a0679ea12088fb8ed6b27a5784102800d8ab40fc6e58  MasterLooter.asi
